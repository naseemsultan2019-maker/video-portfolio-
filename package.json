{
  "name": "video-portfolio",
  "private": true,
  "description": "Static single-page video portfolio",
  "scripts": {
    "lint": "html-validate index.html",
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "html-validate": "^9.0.0"
  }
}
